export const metadata = {
  title: 'Webform Test — Backtalk',
  description: 'Form targeting test playground',
}

export default function WebformLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
